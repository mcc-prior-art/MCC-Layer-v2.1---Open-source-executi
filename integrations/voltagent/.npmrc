# VoltAgent's peer-dependency graph (e.g. ollama-ai-provider-v2 wants zod@^4
# while the integration pins zod@^3.25) triggers ERESOLVE under npm's strict peer
# resolution. Use legacy peer resolution consistently for install AND `npm ci`
# (workflow + Docker image) so the committed lockfile stays reproducible.
legacy-peer-deps=true
