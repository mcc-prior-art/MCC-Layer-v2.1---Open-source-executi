name: MCC Runtime CI

on:
  push:
  pull_request:

# Least privilege: this workflow only reads the repository and runs tests/smoke
# checks — it never writes to the repo, comments, or publishes artifacts. The
# read-only default is set at the top level and inherited by every job.
permissions:
  contents: read

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install Redis (tests/test_assurance_scaffolding.py drives Workstream E's real cross-instance replay resistance)
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends redis-server redis-tools

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Run tests
        run: pytest tests/ -v

  normative-v1-conformance:
    # MCC Normative v1.0 — Implementation Conformance Baseline. Recomputes the
    # requirement inventory directly from specs/MCC-{CP,EB,CM,TC}-001.md (never
    # trusts the committed artifacts), validates structure/schema/baseline
    # provenance, confirms the committed conformance/normative-v1.0/ artifacts
    # are byte-identical to a fresh generation (so a stale, uncommitted
    # regeneration fails CI), and runs the dedicated test suite. This baseline
    # is observational: it does not certify MCC-Core or any adapter — see
    # conformance/normative-v1.0/README.md.
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: "src:."
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Validate the Normative v1.0 conformance baseline (fail-closed)
        run: python -m mcc_conformance validate

      - name: Conformance baseline tests
        run: |
          pytest tests/test_conformance_baseline.py -v
          pytest tests/test_conformance_extraction_correction.py -v

      - name: Confirm committed artifacts are not stale (regenerate + diff)
        run: |
          python -m mcc_conformance generate
          git diff --exit-code -- conformance/normative-v1.0/ || {
            echo "STOP: conformance/normative-v1.0/ artifacts are stale."
            echo "Run: python -m mcc_conformance generate, then commit the result."
            exit 1
          }
          echo "OK: conformance artifacts are deterministic and up to date."

      - name: Upload conformance baseline artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: normative-v1-conformance-baseline
          path: conformance/normative-v1.0/
          if-no-files-found: error

  certification-pipeline:
    # MCC End-to-End Certification Pipeline (PR #67): mcc_certify orchestrates
    # the complete MCC-CP-001 chain (Conformance Run -> Evidence Bundle ->
    # Certification Manifest -> Technical Certificate -> Offline Verification)
    # over the existing Wave A/B/C mcc_evidence producers/verifiers, unchanged.
    # This job installs the full dev dependency set (mcc_certify depends on
    # mcc_evidence/mcc_core/mcc_conformance, none of which ship in the curated
    # mcc-core PyPI wheel -- see docs/CERTIFICATION_PIPELINE.md, "CLI", for why),
    # runs the deterministic reference-fixture certification twice to prove
    # reproducibility, independently re-verifies the resulting run offline, runs
    # the full tamper-negative suite, and uploads the generated run as a CI
    # artifact. No allow-failure; a stage failure, a verification failure, or
    # nondeterminism between the two runs fails this job.
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: "src:."
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Package/import smoke test
        run: |
          python -c "import mcc_certify; print(mcc_certify.CertificationPipeline, mcc_certify.resolve_target)"
          python -m mcc_certify list-targets

      - name: Certification pipeline unit + architecture-guard tests
        run: |
          pytest tests/test_mcc_certify_pipeline.py -v
          pytest tests/test_mcc_certify_architecture_guards.py -v

      - name: Deterministic reference certification (run 1 of 2)
        run: |
          python -m mcc_certify certify reference-fixture \
            --output artifacts/certification-ci/run1 \
            --run-id ci-reference-run \
            --timestamp 2026-07-26T00:00:00Z \
            --format json | tee /tmp/ci-run1-result.json
          test "$(python -c 'import json,sys; print(json.load(open("/tmp/ci-run1-result.json"))["outcome"])')" = "CERTIFIED"

      - name: Deterministic reference certification (run 2 of 2, independent output dir)
        run: |
          python -m mcc_certify certify reference-fixture \
            --output artifacts/certification-ci/run2 \
            --run-id ci-reference-run \
            --timestamp 2026-07-26T00:00:00Z \
            --format json | tee /tmp/ci-run2-result.json
          test "$(python -c 'import json,sys; print(json.load(open("/tmp/ci-run2-result.json"))["outcome"])')" = "CERTIFIED"

      - name: Confirm deterministic regeneration (byte-identical artifacts across both runs)
        run: |
          diff -rq \
            artifacts/certification-ci/run1/reference-fixture/ci-reference-run \
            artifacts/certification-ci/run2/reference-fixture/ci-reference-run \
          && echo "OK: both runs produced byte-identical artifacts" || {
            echo "STOP: nondeterministic certification run output"; exit 1;
          }

      - name: Independent offline verification of the resulting run
        run: |
          python -m mcc_certify verify \
            artifacts/certification-ci/run1/reference-fixture/ci-reference-run --format json \
          | tee /tmp/ci-verify-result.json
          test "$(python -c 'import json; print(json.load(open("/tmp/ci-verify-result.json"))["valid"])')" = "True"

      - name: Upload the test certification run as a CI artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mcc-certify-reference-run
          path: artifacts/certification-ci/
          if-no-files-found: error

  certification-trust-publication:
    # MCC Certification Trust Anchor, Issuer Key Management & Publication
    # Foundation (PR #68): the Issuer Identity model, offline Trust Store,
    # Signing-Key Provider contract, static Publication mechanism, and
    # trusted offline verification against an explicit trust-anchor set —
    # all built above the unchanged PR #67 mcc_certify pipeline. This job
    # generates a throwaway Ed25519 issuer key AT RUNTIME (never committed
    # to the repository, never a production key), builds an Issuer Identity
    # + Trust Store from it, certifies the reference fixture through the
    # packaged CLI in OFFICIAL mode, independently re-verifies the result
    # against the explicit trust store + publication index, greps every
    # generated artifact for private-key material (fails the job if any is
    # found), confirms mcc_certify remains excluded from the curated
    # mcc-core wheel (no packaging regression), and uploads only the
    # non-secret run/publication artifacts — never the private key file.
    # No allow-failure.
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: "src:."
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies (+ build for the packaging regression check)
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
          pip install build

      - name: Package/import smoke test (PR #68 public API)
        run: |
          python -c "from mcc_certify import (IssuerIdentity, TrustStore, LocalFileSigningKeyProvider, \
            FixtureSigningKeyProvider, PublicationRecord, PublicationIndex, \
            verify_certification_run_trusted, build_issuer_identity, build_trust_store); \
            print('PR #68 public API OK')"

      - name: Trust/Issuer/Publication unit tests + architecture guards
        run: |
          pytest tests/test_mcc_certify_pr68.py -v
          pytest tests/test_mcc_certify_trust_publication_guards.py -v
          pytest tests/test_mcc_certify_architecture_guards.py -v
          pytest tests/test_mcc_certify_pipeline.py -v

      - name: Packaging regression — mcc_certify still excluded from the curated wheel
        run: |
          python -m build --wheel --sdist --outdir /tmp/mcc-certify-ci-dist .
          python - <<'PYEOF'
          import zipfile, glob, sys
          wheel = sorted(glob.glob("/tmp/mcc-certify-ci-dist/*.whl"))[-1]
          with zipfile.ZipFile(wheel) as zf:
              names = zf.namelist()
          leaked = [n for n in names if n.startswith("mcc_certify/") or n.startswith("mcc_evidence/") or n.startswith("mcc_conformance/")]
          if leaked:
              print("STOP: curated wheel unexpectedly ships governance-adjacent packages:", leaked)
              sys.exit(1)
          print("OK: curated wheel does not ship mcc_certify/mcc_evidence/mcc_conformance")
          PYEOF

      - name: Generate a throwaway (non-production) Issuer signing key
        run: |
          mkdir -p /tmp/mcc-certify-ci-trust
          python - <<'PYEOF'
          from cryptography.hazmat.primitives import serialization
          from mcc_core.signing import SigningKey
          from mcc_certify.issuer import build_issuer_identity, write_issuer_identity
          from mcc_certify.trust_store import build_trust_store, write_trust_store

          key = SigningKey.generate("ci-issuer-key-1")
          with open("/tmp/mcc-certify-ci-trust/issuer-key.pem", "wb") as f:
              f.write(key._private_key.private_bytes(
                  serialization.Encoding.PEM, serialization.PrivateFormat.PKCS8, serialization.NoEncryption(),
              ))
          issuer = build_issuer_identity(
              issuer_id="mcc-ci-issuer", display_name="MCC Runtime CI Throwaway Issuer",
              key_id="ci-issuer-key-1", public_key=key.public_key(),
              valid_from=1785024000, created_at=1785024000,
          )
          write_issuer_identity(issuer, "/tmp/mcc-certify-ci-trust/issuer.json")
          write_trust_store(build_trust_store((issuer,)), "/tmp/mcc-certify-ci-trust/trust-store.json")
          print("CI throwaway issuer + trust store generated (never committed)")
          PYEOF

      - name: Official-mode certification through the CLI
        run: |
          python -m mcc_certify certify reference-fixture \
            --output artifacts/certification-trust-ci/run \
            --run-id ci-official-run \
            --timestamp 2026-07-26T00:00:00Z \
            --issuer-config /tmp/mcc-certify-ci-trust/issuer.json \
            --signing-key /tmp/mcc-certify-ci-trust/issuer-key.pem \
            --trust-store /tmp/mcc-certify-ci-trust/trust-store.json \
            --publication-dir artifacts/certification-trust-ci/publication \
            --format json | tee /tmp/ci-official-result.json
          test "$(python -c 'import json; print(json.load(open("/tmp/ci-official-result.json"))["outcome"])')" = "CERTIFIED"

      - name: Trusted offline re-verification against the explicit trust store
        run: |
          python -m mcc_certify verify \
            artifacts/certification-trust-ci/run/reference-fixture/ci-official-run \
            --trust-store /tmp/mcc-certify-ci-trust/trust-store.json \
            --publication-index artifacts/certification-trust-ci/publication/publication-index.json \
            --require-published --format json | tee /tmp/ci-official-verify.json
          test "$(python -c 'import json; print(json.load(open("/tmp/ci-official-verify.json"))["valid"])')" = "True"

      - name: Tamper-negative check — wrong trust store must fail verification
        run: |
          python - <<'PYEOF'
          import sys
          from cryptography.hazmat.primitives import serialization
          from mcc_core.signing import SigningKey
          from mcc_certify.issuer import build_issuer_identity, write_issuer_identity
          from mcc_certify.trust_store import build_trust_store, write_trust_store
          from mcc_certify.verify import verify_certification_run_trusted

          wrong_key = SigningKey.generate("ci-issuer-key-1")
          wrong_issuer = build_issuer_identity(
              issuer_id="mcc-ci-issuer", display_name="Wrong Issuer", key_id="ci-issuer-key-1",
              public_key=wrong_key.public_key(), valid_from=1785024000, created_at=1785024000,
          )
          wrong_store = build_trust_store((wrong_issuer,))
          report = verify_certification_run_trusted(
              "artifacts/certification-trust-ci/run/reference-fixture/ci-official-run",
              trust_store=wrong_store,
          )
          if report["valid"]:
              print("STOP: verification succeeded against a wrong trust store")
              sys.exit(1)
          print("OK: wrong trust store correctly fails verification")
          PYEOF

      - name: Verify no secrets in generated artifacts
        run: |
          if grep -R "PRIVATE KEY" artifacts/certification-trust-ci/ ; then
            echo "STOP: private key material found in generated certification artifacts"
            exit 1
          fi
          echo "OK: no private key material in artifacts/certification-trust-ci/"

      - name: Upload non-secret certification + publication evidence only
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mcc-certify-trust-publication-evidence
          path: artifacts/certification-trust-ci/
          if-no-files-found: error

  protocol:
    # Canonical Governance Protocol & Canonical Ingress Pipeline (PR #49).
    # Offline unit tests + architecture/reconciliation guards prove one normative
    # wire contract (reconciled with PR #43), CanonicalProposal/GovernanceDecision,
    # the mandatory in-process ingress pipeline, version negotiation (fail-closed),
    # and that adapters cannot bypass the pipeline/gateway/audit or authorize
    # locally. Then generates the reproducible ingress evidence (every registered
    # adapter -> CanonicalProposal -> pipeline -> real gateway -> GovernanceDecision).
    runs-on: ubuntu-latest
    env:
      MCC_USE_OPA: "false"
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Protocol unit tests + architecture/reconciliation guards
        run: pytest tests/protocol/ -v

      - name: Canonical Ingress evidence (proposal -> pipeline -> real gateway)
        run: pytest tests/interoperability/test_canonical_ingress.py -v

      - name: Upload canonical ingress evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: canonical-ingress-evidence
          path: artifacts/protocol/
          if-no-files-found: warn

  adapter-sdk:
    # Adapter SDK (PR #50) — the public, framework-neutral SDK third parties use to
    # build MCC-Core adapters. Runs the SDK unit tests + architecture guards + the
    # import-boundary regression (importing the SDK/protocol never loads the
    # governance engine), the minimal example, deterministic evidence generation,
    # the migrated reference adapter's interoperability, and the packaged clean
    # install of the curated (engine-free) wheel. Offline; no external services.
    runs-on: ubuntu-latest
    env:
      MCC_USE_OPA: "false"
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies (+ build for the packaging checks)
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
          pip install build

      - name: Adapter SDK tests + architecture guards + reference-migration interop
        run: pytest tests/adapter_sdk/ -v

      - name: Import-boundary regression (SDK/protocol import loads no engine)
        run: pytest tests/adapter_sdk/test_import_boundary.py -v

      - name: Minimal example (no execution, no credentials, no side effects)
        run: python examples/adapter_sdk/minimal_adapter.py
        env:
          PYTHONPATH: "src:sdk/python/src:."

      - name: Deterministic evidence generation (generate twice, compare)
        run: |
          python -c "import examples.adapter_sdk.minimal_adapter as m; \
            from mcc_adapter_sdk import generate_adapter_evidence; \
            a=m.GreetingAdapter(); \
            e1=generate_adapter_evidence(a, write=False); \
            e2=generate_adapter_evidence(a, write=False); \
            assert e1==e2, 'evidence not deterministic'; print('evidence deterministic OK')"
        env:
          PYTHONPATH: "src:sdk/python/src:."

      - name: Public API import smoke
        run: |
          python -c "from mcc_adapter_sdk import (Adapter, AdapterMetadata, AdapterContext, \
            AdapterRunner, AdapterRunResult, AdapterRegistration, AdapterValidationReport, \
            register_adapter, validate_adapter, generate_adapter_evidence, SDK_VERSION); \
            print('curated public API OK', SDK_VERSION)"
        env:
          PYTHONPATH: "src:sdk/python/src:."

      - name: Packaging — build wheel/sdist, inspect content, clean install, run example
        run: pytest tests/test_artifact_contract.py -v

      - name: Upload Adapter SDK evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: adapter-sdk-evidence
          path: artifacts/adapter-sdk/
          if-no-files-found: warn

  compliance:
    # Integration Contract Compliance & Certification Suite (PR #44).
    # Runs the suite's unit + integration tests, then certifies the reference and
    # VoltAgent adapters through the SAME framework-neutral boundary and asserts a
    # deliberately non-compliant adapter is rejected. Reports are uploaded as
    # artifacts. Offline and deterministic; no external services, no secrets.
    runs-on: ubuntu-latest
    env:
      # The CLI steps run without conftest, so set the source path and keep the
      # gateway/audit off the prior-art chain explicitly.
      PYTHONPATH: "src:sdk/python/src:."
      MCC_USE_OPA: "false"
      MCC_AUDIT_LOG_PATH: "/tmp/mcc-compliance/audit.jsonl"
      MCC_GATEWAY_AUDIT_LOG_PATH: "/tmp/mcc-compliance/gw-audit.jsonl"
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Prepare isolated audit directory
        run: mkdir -p /tmp/mcc-compliance

      - name: Compliance suite unit + integration tests
        run: pytest tests/test_compliance_suite.py -v

      - name: Certified Adapter Program tests
        run: pytest tests/test_certified_adapter_program.py -v

      - name: Governance Capability Profile tests
        run: pytest tests/test_capability_profile.py -v

      - name: Validate the reference capability profiles (schema + semantics)
        run: |
          for f in minimal-valid full-capable unsupported-optional voltagent-reference-example; do
            python -m mcc_compliance validate-capabilities "tests/capability_profiles/$f.json"
          done
          # A deliberately invalid profile must be rejected (non-zero).
          if python -m mcc_compliance validate-capabilities \
               tests/capability_profiles/invalid-overlap.json; then
            echo "::error::an invalid capability profile was accepted"; exit 1
          fi
          echo "OK: valid profiles accepted, invalid profile rejected"

      - name: Certify the reference adapter (must be CERTIFIED)
        run: |
          python -m mcc_compliance certify --adapter reference \
            --contract-version 1.0 --output-dir artifacts/compliance
          # Exit 0 == CERTIFIED (the step fails otherwise).

      - name: Certify VoltAgent through the generic boundary (must be CERTIFIED)
        run: |
          python -m mcc_compliance certify --adapter voltagent \
            --contract-version 1.0 --output-dir artifacts/compliance

      - name: Verify the committed certification manifest is not stale or tampered
        run: |
          # Regenerates certification from real evidence and compares it to the
          # committed certifications/manifest.json. Fails (non-zero) if any official
          # adapter no longer conforms, or a digest/status/count was hand-edited.
          python -m mcc_compliance verify-manifest

      - name: A deliberately non-compliant adapter must FAIL certification
        run: |
          # Registers an always-allow double and asserts the CLI rejects it
          # (non-zero exit). If it were incorrectly certified, this step fails.
          python - <<'PY'
          import sys
          from mcc_compliance.models import AdapterMetadata, AdapterOutcome
          from mcc_compliance.registry import register_adapter
          from mcc_compliance.cli import main
          from mcc_client.contract import CONTRACT_VERSION

          class AlwaysAllow:
              def describe(self):
                  return AdapterMetadata("ci-always-allow", "0.0.1", "ci/always-allow",
                                         CONTRACT_VERSION, None)
              def run_scenario(self, ctx):
                  return AdapterOutcome(verdict="ALLOW", executed=True, audit_valid=True)

          register_adapter("ci-always-allow", AlwaysAllow)
          rc = main(["certify", "--adapter", "ci-always-allow", "--contract-version", "1.0"])
          assert rc != 0, "FAIL: a non-compliant adapter was incorrectly certified"
          print(f"OK: non-compliant adapter rejected (exit {rc})")
          PY

      - name: Adapter-SDK certification tests (PR #51 — SDK is the canonical ingress)
        run: pytest tests/test_sdk_certification.py -v

      - name: Certify the reference adapter THROUGH the Adapter SDK (must be CERTIFIED)
        run: |
          python -m mcc_compliance certify-sdk --contract-version 1.0 \
            --output-dir artifacts/certification
          # Exit 0 == CERTIFIED (the step fails otherwise).

      - name: Certification outputs are reproducible (generate twice, compare)
        run: |
          python -m mcc_compliance certify-sdk --contract-version 1.0 \
            --output-dir artifacts/certification-rerun
          for f in certification.json evidence.json certification-report.md; do
            if ! diff -q "artifacts/certification/$f" "artifacts/certification-rerun/$f"; then
              echo "::error::certification output $f is not reproducible"; exit 1
            fi
          done
          echo "OK: certification.json / evidence.json / certification-report.md reproducible"

      - name: Upload compliance reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: compliance-reports-ic-1.0-suite-1.0.0
          path: artifacts/compliance/
          if-no-files-found: warn

      - name: Upload SDK certification outputs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: adapter-sdk-certification
          path: artifacts/certification/
          if-no-files-found: warn

  interoperability:
    # Multi-Adapter Interoperability Proof — 48a foundation.
    # Boots ONE real out-of-process MCC Gateway and drives the framework-neutral
    # HTTP adapter (5/5) through the seven common governance scenarios over a real
    # HTTP boundary, with static bypass guards and a deterministic evidence bundle.
    # The four real-framework adapters (VoltAgent/LangGraph/CrewAI/AutoGen) are
    # added in 48b–e with their own isolated dependency installs. Offline, no LLM.
    runs-on: ubuntu-latest
    env:
      MCC_USE_OPA: "false"
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Interoperability proof (shared out-of-process Gateway, real HTTP)
        run: pytest tests/interoperability/ -v

      - name: Upload interoperability evidence bundle
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: interoperability-evidence-48a
          path: artifacts/interoperability/
          if-no-files-found: warn

  interop-langgraph:
    # 48b — real LangGraph adapter (2/5) through the SAME shared Gateway. Installs
    # the official langgraph distribution as an isolated optional dependency and
    # asserts it is genuinely present and exercised (a native CompiledStateGraph is
    # built and invoked). A missing/absent framework fails the job — it is never
    # silently skipped here. Offline, no LLM, no external API keys.
    runs-on: ubuntu-latest
    env:
      MCC_USE_OPA: "false"
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies (+ isolated langgraph)
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
          pip install -r tests/interoperability/requirements-langgraph.txt

      - name: Show resolved framework version
        run: python -c "import importlib.metadata as m; print('langgraph', m.version('langgraph'))"

      - name: Assert the official LangGraph package is really importable
        run: python -c "import langgraph; from langgraph.graph import StateGraph; from langgraph.graph.state import CompiledStateGraph; print('langgraph native API present')"

      - name: Interoperability proof WITH the real LangGraph adapter
        # test_framework_provenance is NOT skipped here (langgraph installed): it
        # fails if the adapter is missing or not a genuine native integration.
        run: pytest tests/interoperability/ -v

      - name: Upload interoperability evidence bundle (with LangGraph)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: interoperability-evidence-48b-langgraph
          path: artifacts/interoperability/
          if-no-files-found: warn

  interop-autogen:
    # 48c — real AutoGen adapter (3/5) through the SAME shared Gateway. Installs the
    # official Microsoft AutoGen v0.4+ distributions (autogen-agentchat + autogen-core)
    # as isolated optional dependencies and asserts they are genuinely present and
    # exercised (a native RoutedAgent runs on a SingleThreadedAgentRuntime). A
    # missing/absent framework fails the job — never silently skipped. Offline, no LLM.
    runs-on: ubuntu-latest
    env:
      MCC_USE_OPA: "false"
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies (+ isolated autogen)
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
          pip install -r tests/interoperability/requirements-autogen.txt

      - name: Show resolved framework versions
        run: python -c "import importlib.metadata as m; print('autogen-agentchat', m.version('autogen-agentchat'), '| autogen-core', m.version('autogen-core'))"

      - name: Assert the official AutoGen packages are really importable
        run: python -c "import autogen_core; from autogen_core import RoutedAgent, SingleThreadedAgentRuntime; print('autogen native API present')"

      - name: Interoperability proof WITH the real AutoGen adapter
        run: pytest tests/interoperability/ -v

      - name: Upload interoperability evidence bundle (with AutoGen)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: interoperability-evidence-48c-autogen
          path: artifacts/interoperability/
          if-no-files-found: warn

  interop-crewai:
    # 48d — real CrewAI adapter (4/5) through the SAME shared Gateway. Installs the
    # official crewai distribution as an isolated optional dependency (heavy — it
    # pulls a large transitive tree) and asserts it is genuinely present and
    # exercised (a native crewai.flow.flow.Flow is built and run via kickoff). A
    # missing/absent framework fails the job — never silently skipped here. The
    # native run is kept fully offline: CrewAI telemetry/tracing is opted out (env
    # below + the adapter opts out again before importing crewai). No LLM, no keys.
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      MCC_USE_OPA: "false"
      CREWAI_DISABLE_TELEMETRY: "true"
      CREWAI_TELEMETRY_OPT_OUT: "true"
      CREWAI_TRACING_ENABLED: "false"
      OTEL_SDK_DISABLED: "true"
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies (+ isolated crewai)
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
          pip install -r tests/interoperability/requirements-crewai.txt

      - name: Show resolved framework version
        run: python -c "import importlib.metadata as m; print('crewai', m.version('crewai'))"

      - name: Assert the official CrewAI package is really importable
        run: python -c "import crewai; from crewai.flow.flow import Flow, start, listen; print('crewai native API present')"

      - name: Interoperability proof WITH the real CrewAI adapter
        # test_framework_provenance is NOT skipped here (crewai installed): it fails
        # if the adapter is missing or not a genuine native integration.
        run: pytest tests/interoperability/ -v

      - name: Upload interoperability evidence bundle (with CrewAI)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: interoperability-evidence-48d-crewai
          path: artifacts/interoperability/
          if-no-files-found: warn

  interop-voltagent:
    # 48e — real VoltAgent adapter (5/5, completes the matrix) through the SAME
    # shared Gateway. VoltAgent is a Node/TypeScript framework, so this job installs
    # Node + the real integrations/voltagent package and the adapter exercises a
    # genuine native @voltagent/core Agent in a Node subprocess (deterministic
    # offline model — no LLM, no key, no network) to originate the proposal. A
    # missing Node toolchain or @voltagent/core fails the job — never silently
    # skipped here. The four Python framework adapters are deliberately NOT installed
    # together with each other or here: crewai and autogen pin mutually incompatible
    # pydantic versions, so the frameworks cannot share one process — which is why
    # every framework is proven in its own isolated job over the identical shared
    # governance path.
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      MCC_USE_OPA: "false"
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install Python dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Install the real VoltAgent package (reproducible)
        working-directory: integrations/voltagent
        run: npm ci --no-audit --no-fund

      - name: Show resolved framework version
        run: node -e "console.log('@voltagent/core', require('./integrations/voltagent/node_modules/@voltagent/core/package.json').version)"

      - name: Assert the official VoltAgent package + Node runner are really present
        working-directory: integrations/voltagent
        run: node -e "require('@voltagent/core').Agent; require('fs').accessSync('node_modules/.bin/tsx'); console.log('voltagent native API + tsx present')"

      - name: Interoperability proof WITH the real VoltAgent adapter
        # test_framework_provenance is NOT skipped here (node + @voltagent/core
        # present): it fails if the adapter is missing or not a genuine native
        # integration. Only generic-http + voltagent register in this job.
        run: pytest tests/interoperability/ -v

      - name: Upload interoperability evidence bundle (with VoltAgent)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: interoperability-evidence-48e-voltagent
          path: artifacts/interoperability/
          if-no-files-found: warn

  # ---------------------------------------------------------------------------
  # PR #69 — Official Certification of the Five Reference Ecosystems.
  #
  # Each certification-<ecosystem> job below certifies exactly ONE production
  # reference ecosystem through the identical `mcc certify <target>` pipeline
  # (PR #67/#68), reusing that ecosystem's REAL, unmodified PR #48 adapter --
  # never a mock, stub, or metadata-only descriptor. Every job:
  #   1. installs that ecosystem's real, isolated framework dependency (the
  #      SAME isolation as interop-langgraph/-autogen/-crewai/-voltagent above
  #      -- these frameworks are never installed together in one job/process);
  #   2. asserts the real native package/API is genuinely importable;
  #   3. generates a DISPOSABLE, clearly-labelled non-production CI candidate
  #      Issuer signing key at runtime (deterministic across jobs so the
  #      aggregate job's shared Trust Store recognizes all five signatures --
  #      this is a disclosed test convenience, never claimed official);
  #   4. certifies the target in official mode against that CI candidate
  #      Issuer/Trust Store (exercising the full official-mode code path)
  #      and independently re-verifies the result;
  #   5. scans every generated artifact for private-key material;
  #   6. uploads only the resulting NON-OFFICIAL candidate certification
  #      artifacts (never the private key).
  # No job in this block ever claims OFFICIAL certification -- that requires
  # the real, separately-protected Issuer key (see
  # .github/workflows/mcc-official-certification.yml and
  # docs/CERTIFICATION_FIVE_ECOSYSTEMS.md, "Candidate versus official status").
  # ---------------------------------------------------------------------------

  certification-generic-http:
    # generic-http has no optional framework dependency -- always runnable.
    runs-on: ubuntu-latest
    env:
      MCC_USE_OPA: "false"
      PYTHONPATH: "src:.:sdk/python/src"
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Generate the disposable, non-production CI candidate Issuer
        run: python scripts/generate_ci_candidate_issuer.py --output-dir /tmp/mcc-ci-trust

      - name: Certify generic-http (candidate, official-mode code path, disposable issuer)
        run: |
          python -m mcc_certify certify generic-http \
            --output artifacts/certification-candidate \
            --run-id ci-candidate-run --timestamp 2026-07-26T00:00:00Z \
            --issuer-config /tmp/mcc-ci-trust/issuer.json \
            --signing-key /tmp/mcc-ci-trust/issuer-key.pem \
            --trust-store /tmp/mcc-ci-trust/trust-store.json \
            --publication-dir artifacts/certification-candidate/publication \
            --format json | tee /tmp/certify-result.json
          test "$(python -c 'import json; print(json.load(open("/tmp/certify-result.json"))["outcome"])')" = "CERTIFIED"

      - name: Independently re-verify the candidate certification
        run: |
          python -m mcc_certify verify \
            artifacts/certification-candidate/generic-http/ci-candidate-run \
            --trust-store /tmp/mcc-ci-trust/trust-store.json --format json | tee /tmp/verify-result.json
          test "$(python -c 'import json; print(json.load(open("/tmp/verify-result.json"))["valid"])')" = "True"

      - name: Scan candidate artifacts for private-key material
        run: |
          if grep -R "PRIVATE KEY" artifacts/certification-candidate/ ; then
            echo "STOP: private key material found in candidate certification artifacts"; exit 1
          fi
          echo "OK: no private key material in candidate artifacts"

      - name: Upload NON-OFFICIAL candidate certification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: certification-candidate-generic-http
          path: artifacts/certification-candidate/
          if-no-files-found: error

  certification-langgraph:
    runs-on: ubuntu-latest
    env:
      MCC_USE_OPA: "false"
      PYTHONPATH: "src:.:sdk/python/src"
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies (+ isolated langgraph)
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
          pip install -r tests/interoperability/requirements-langgraph.txt

      - name: Assert the official LangGraph package is really importable
        run: python -c "import langgraph; from langgraph.graph import StateGraph; print('langgraph native API present')"

      - name: Generate the disposable, non-production CI candidate Issuer
        run: python scripts/generate_ci_candidate_issuer.py --output-dir /tmp/mcc-ci-trust

      - name: Certify langgraph (candidate, official-mode code path, disposable issuer)
        run: |
          python -m mcc_certify certify langgraph \
            --output artifacts/certification-candidate \
            --run-id ci-candidate-run --timestamp 2026-07-26T00:00:00Z \
            --issuer-config /tmp/mcc-ci-trust/issuer.json \
            --signing-key /tmp/mcc-ci-trust/issuer-key.pem \
            --trust-store /tmp/mcc-ci-trust/trust-store.json \
            --publication-dir artifacts/certification-candidate/publication \
            --format json | tee /tmp/certify-result.json
          test "$(python -c 'import json; print(json.load(open("/tmp/certify-result.json"))["outcome"])')" = "CERTIFIED"

      - name: Independently re-verify the candidate certification
        run: |
          python -m mcc_certify verify \
            artifacts/certification-candidate/langgraph/ci-candidate-run \
            --trust-store /tmp/mcc-ci-trust/trust-store.json --format json | tee /tmp/verify-result.json
          test "$(python -c 'import json; print(json.load(open("/tmp/verify-result.json"))["valid"])')" = "True"

      - name: Scan candidate artifacts for private-key material
        run: |
          if grep -R "PRIVATE KEY" artifacts/certification-candidate/ ; then
            echo "STOP: private key material found in candidate certification artifacts"; exit 1
          fi

      - name: Upload NON-OFFICIAL candidate certification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: certification-candidate-langgraph
          path: artifacts/certification-candidate/
          if-no-files-found: error

  certification-crewai:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      MCC_USE_OPA: "false"
      CREWAI_DISABLE_TELEMETRY: "true"
      CREWAI_TELEMETRY_OPT_OUT: "true"
      CREWAI_TRACING_ENABLED: "false"
      OTEL_SDK_DISABLED: "true"
      PYTHONPATH: "src:.:sdk/python/src"
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies (+ isolated crewai)
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
          pip install -r tests/interoperability/requirements-crewai.txt

      - name: Assert the official CrewAI package is really importable
        run: python -c "import crewai; from crewai.flow.flow import Flow; print('crewai native API present')"

      - name: Generate the disposable, non-production CI candidate Issuer
        run: python scripts/generate_ci_candidate_issuer.py --output-dir /tmp/mcc-ci-trust

      - name: Certify crewai (candidate, official-mode code path, disposable issuer)
        run: |
          python -m mcc_certify certify crewai \
            --output artifacts/certification-candidate \
            --run-id ci-candidate-run --timestamp 2026-07-26T00:00:00Z \
            --issuer-config /tmp/mcc-ci-trust/issuer.json \
            --signing-key /tmp/mcc-ci-trust/issuer-key.pem \
            --trust-store /tmp/mcc-ci-trust/trust-store.json \
            --publication-dir artifacts/certification-candidate/publication \
            --format json | tee /tmp/certify-result.json
          test "$(python -c 'import json; print(json.load(open("/tmp/certify-result.json"))["outcome"])')" = "CERTIFIED"

      - name: Independently re-verify the candidate certification
        run: |
          python -m mcc_certify verify \
            artifacts/certification-candidate/crewai/ci-candidate-run \
            --trust-store /tmp/mcc-ci-trust/trust-store.json --format json | tee /tmp/verify-result.json
          test "$(python -c 'import json; print(json.load(open("/tmp/verify-result.json"))["valid"])')" = "True"

      - name: Scan candidate artifacts for private-key material
        run: |
          if grep -R "PRIVATE KEY" artifacts/certification-candidate/ ; then
            echo "STOP: private key material found in candidate certification artifacts"; exit 1
          fi

      - name: Upload NON-OFFICIAL candidate certification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: certification-candidate-crewai
          path: artifacts/certification-candidate/
          if-no-files-found: error

  certification-autogen:
    runs-on: ubuntu-latest
    env:
      MCC_USE_OPA: "false"
      PYTHONPATH: "src:.:sdk/python/src"
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies (+ isolated autogen)
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
          pip install -r tests/interoperability/requirements-autogen.txt

      - name: Assert the official AutoGen packages are really importable
        run: python -c "import autogen_core; from autogen_core import RoutedAgent; print('autogen native API present')"

      - name: Generate the disposable, non-production CI candidate Issuer
        run: python scripts/generate_ci_candidate_issuer.py --output-dir /tmp/mcc-ci-trust

      - name: Certify autogen (candidate, official-mode code path, disposable issuer)
        run: |
          python -m mcc_certify certify autogen \
            --output artifacts/certification-candidate \
            --run-id ci-candidate-run --timestamp 2026-07-26T00:00:00Z \
            --issuer-config /tmp/mcc-ci-trust/issuer.json \
            --signing-key /tmp/mcc-ci-trust/issuer-key.pem \
            --trust-store /tmp/mcc-ci-trust/trust-store.json \
            --publication-dir artifacts/certification-candidate/publication \
            --format json | tee /tmp/certify-result.json
          test "$(python -c 'import json; print(json.load(open("/tmp/certify-result.json"))["outcome"])')" = "CERTIFIED"

      - name: Independently re-verify the candidate certification
        run: |
          python -m mcc_certify verify \
            artifacts/certification-candidate/autogen/ci-candidate-run \
            --trust-store /tmp/mcc-ci-trust/trust-store.json --format json | tee /tmp/verify-result.json
          test "$(python -c 'import json; print(json.load(open("/tmp/verify-result.json"))["valid"])')" = "True"

      - name: Scan candidate artifacts for private-key material
        run: |
          if grep -R "PRIVATE KEY" artifacts/certification-candidate/ ; then
            echo "STOP: private key material found in candidate certification artifacts"; exit 1
          fi

      - name: Upload NON-OFFICIAL candidate certification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: certification-candidate-autogen
          path: artifacts/certification-candidate/
          if-no-files-found: error

  certification-voltagent:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      MCC_USE_OPA: "false"
      PYTHONPATH: "src:.:sdk/python/src"
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install Python dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Install the real VoltAgent package (reproducible)
        working-directory: integrations/voltagent
        run: npm ci --no-audit --no-fund

      - name: Assert the official VoltAgent package + Node runner are really present
        working-directory: integrations/voltagent
        run: node -e "require('@voltagent/core').Agent; require('fs').accessSync('node_modules/.bin/tsx'); console.log('voltagent native API + tsx present')"

      - name: Generate the disposable, non-production CI candidate Issuer
        run: python scripts/generate_ci_candidate_issuer.py --output-dir /tmp/mcc-ci-trust

      - name: Certify voltagent (candidate, official-mode code path, disposable issuer)
        run: |
          python -m mcc_certify certify voltagent \
            --output artifacts/certification-candidate \
            --run-id ci-candidate-run --timestamp 2026-07-26T00:00:00Z \
            --issuer-config /tmp/mcc-ci-trust/issuer.json \
            --signing-key /tmp/mcc-ci-trust/issuer-key.pem \
            --trust-store /tmp/mcc-ci-trust/trust-store.json \
            --publication-dir artifacts/certification-candidate/publication \
            --format json | tee /tmp/certify-result.json
          test "$(python -c 'import json; print(json.load(open("/tmp/certify-result.json"))["outcome"])')" = "CERTIFIED"

      - name: Independently re-verify the candidate certification
        run: |
          python -m mcc_certify verify \
            artifacts/certification-candidate/voltagent/ci-candidate-run \
            --trust-store /tmp/mcc-ci-trust/trust-store.json --format json | tee /tmp/verify-result.json
          test "$(python -c 'import json; print(json.load(open("/tmp/verify-result.json"))["valid"])')" = "True"

      - name: Scan candidate artifacts for private-key material
        run: |
          if grep -R "PRIVATE KEY" artifacts/certification-candidate/ ; then
            echo "STOP: private key material found in candidate certification artifacts"; exit 1
          fi

      - name: Upload NON-OFFICIAL candidate certification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: certification-candidate-voltagent
          path: artifacts/certification-candidate/
          if-no-files-found: error

  certification-five-ecosystems-aggregate:
    # All-or-nothing: this job requires all five certification-<ecosystem>
    # jobs above to have succeeded (GitHub Actions' default `needs:` behavior
    # skips this job -- and therefore fails the required check -- if any one
    # of them failed; there is no continue-on-error anywhere in this block).
    needs:
      - certification-generic-http
      - certification-langgraph
      - certification-crewai
      - certification-autogen
      - certification-voltagent
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: "src:.:sdk/python/src"
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Regenerate the SAME disposable CI candidate Issuer/Trust Store
        run: python scripts/generate_ci_candidate_issuer.py --output-dir /tmp/mcc-ci-trust

      - uses: actions/download-artifact@v4
        with:
          pattern: certification-candidate-*
          path: /tmp/candidate-runs
          merge-multiple: false

      - name: Build the all-or-nothing aggregate five-ecosystem Publication Set
        run: |
          python -m mcc_certify aggregate-publish \
            --trust-store /tmp/mcc-ci-trust/trust-store.json \
            --publication-dir artifacts/aggregate-candidate \
            --run generic-http=/tmp/candidate-runs/certification-candidate-generic-http/generic-http/ci-candidate-run \
            --run langgraph=/tmp/candidate-runs/certification-candidate-langgraph/langgraph/ci-candidate-run \
            --run crewai=/tmp/candidate-runs/certification-candidate-crewai/crewai/ci-candidate-run \
            --run autogen=/tmp/candidate-runs/certification-candidate-autogen/autogen/ci-candidate-run \
            --run voltagent=/tmp/candidate-runs/certification-candidate-voltagent/voltagent/ci-candidate-run \
            --format json | tee /tmp/aggregate-result.json
          test "$(python -c 'import json; print(json.load(open("/tmp/aggregate-result.json"))["record_count"])')" = "5"

      - name: Independently re-verify the aggregate Publication Set
        run: |
          python -m mcc_certify verify-aggregate \
            --trust-store /tmp/mcc-ci-trust/trust-store.json \
            --publication-dir artifacts/aggregate-candidate \
            --run generic-http=/tmp/candidate-runs/certification-candidate-generic-http/generic-http/ci-candidate-run \
            --run langgraph=/tmp/candidate-runs/certification-candidate-langgraph/langgraph/ci-candidate-run \
            --run crewai=/tmp/candidate-runs/certification-candidate-crewai/crewai/ci-candidate-run \
            --run autogen=/tmp/candidate-runs/certification-candidate-autogen/autogen/ci-candidate-run \
            --run voltagent=/tmp/candidate-runs/certification-candidate-voltagent/voltagent/ci-candidate-run \
            --format json | tee /tmp/verify-aggregate-result.json
          test "$(python -c 'import json; print(json.load(open("/tmp/verify-aggregate-result.json"))["valid"])')" = "True"

      - name: Scan aggregate artifacts for private-key material
        run: |
          if grep -R "PRIVATE KEY" artifacts/aggregate-candidate/ ; then
            echo "STOP: private key material found in the aggregate publication set"; exit 1
          fi

      - name: Upload NON-OFFICIAL aggregate five-ecosystem Publication Set
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: certification-candidate-five-ecosystem-aggregate
          path: artifacts/aggregate-candidate/
          if-no-files-found: error

  certification-architecture-guards:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: "src:.:sdk/python/src"
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Five-ecosystem certification unit + aggregate + architecture-guard tests
        run: |
          pytest tests/test_mcc_certify_pr69.py -v
          pytest tests/test_mcc_certify_ecosystem_guards.py -v
          pytest tests/test_mcc_certify_pipeline.py -v
          pytest tests/test_mcc_certify_architecture_guards.py -v
          pytest tests/test_mcc_certify_pr68.py -v
          pytest tests/test_mcc_certify_trust_publication_guards.py -v

  certification-package-smoke:
    # Confirms the PR #69 additions do not regress mcc_certify's packaging
    # boundary: it remains excluded from the curated mcc-core wheel (exactly
    # like mcc_conformance/mcc_evidence), and `python -m mcc_certify
    # list-targets` still works under the SAME minimal PYTHONPATH the
    # existing certification-pipeline/certification-trust-publication jobs
    # use ("src:.", no sdk/python/src) -- listing/registering all six targets
    # never requires any ecosystem's optional framework dependency.
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: "src:."
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies (+ build)
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
          pip install build

      - name: mcc_certify still excluded from the curated wheel
        run: |
          python -m build --wheel --sdist --outdir /tmp/mcc-certify-pr69-dist .
          python - <<'PYEOF'
          import zipfile, glob, sys
          wheel = sorted(glob.glob("/tmp/mcc-certify-pr69-dist/*.whl"))[-1]
          with zipfile.ZipFile(wheel) as zf:
              names = zf.namelist()
          leaked = [n for n in names if n.startswith(("mcc_certify/", "mcc_evidence/", "mcc_conformance/"))]
          if leaked:
              print("STOP: curated wheel unexpectedly ships governance-adjacent packages:", leaked)
              sys.exit(1)
          print("OK: curated wheel excludes mcc_certify/mcc_evidence/mcc_conformance")
          PYEOF

      - name: list-targets under the minimal src:. PYTHONPATH (no ecosystem dependency required)
        run: |
          python -m mcc_certify list-targets | tee /tmp/list-targets.out
          for tid in generic-http langgraph crewai autogen voltagent reference-fixture; do
            grep -qx "$tid" /tmp/list-targets.out || { echo "STOP: $tid missing from list-targets"; exit 1; }
          done

  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Egress proxy end-to-end smoke (ALLOW / CONSTRAIN / DENY / ESCALATE)
        run: |
          # Boots upstream + gateway + egress proxy on loopback and drives the
          # full scenario through the proxy. The script asserts ALLOW passes,
          # CONSTRAIN rewrites the body to the mandate cap, and DENY/ESCALATE
          # are blocked before reaching upstream — exiting non-zero on any miss.
          python examples/egress_proxy_demo.py

      - name: Transaction governance end-to-end smoke (idempotency + velocity)
        run: |
          # Boots upstream + gateway + coordinator-backed egress proxy and drives
          # the full a-h ordering: a payment executes once, a duplicate
          # idempotency key never executes again, and four valid payments cannot
          # bypass the cumulative velocity ceiling. Exits non-zero on any miss.
          python examples/transaction_governance_demo.py

      - name: Governance HTTP end-to-end smoke (mandates + approvals)
        run: |
          # Boots the real gateway (with a pilot trust config) + upstream and
          # drives mandate verify/execute/revoke and the ESCALATE approve→execute
          # single-use flow over HTTP. Asserts no blocked decision reaches the
          # upstream (no bypass). Exits non-zero on any miss.
          python examples/governance_http_demo.py

      - name: Pilot reference integration smoke (outbound HTTP, four paths, no bypass)
        run: |
          # An agent's outbound HTTP action governed by the real runtime
          # (consensus-required): ALLOW / DENY / approved-ESCALATE /
          # re-consensused-CONSTRAIN. Proves the original over-cap body is never
          # sent and a direct executor call is refused. Exits non-zero on any miss.
          python examples/pilot_reference_integration.py

      - name: Enforced egress proxy smoke (consensus + re-consensus, no bypass)
        run: |
          # Drives the enforced egress proxy over HTTP: ALLOW / DENY /
          # ESCALATE->approve / CONSTRAIN->new-hash->re-consensus / replay /
          # tamper / no-bypass. Asserts the upstream receives ONLY authorized
          # actions and never the original over-cap body. Exits non-zero on any miss.
          python examples/enforced_egress_agent.py

      - name: Smoke demo shutdown stress (no exit 139, no hang)
        run: |
          # Regression guard for the embedded-server lifecycle: run every demo
          # repeatedly and fail on any non-zero exit, SIGSEGV (exit 139), or hang.
          # Deterministic and bounded (per-run timeout); no retries or masking.
          # 5 demos x 3 = 15 sequential executions (~30s).
          bash scripts/smoke_stress.sh 3

      - name: Validate the pilot Docker Compose configuration
        run: |
          # The pilot compose now includes the egress proxy + isolated networks.
          # Validate it parses/interpolates without standing the stack up.
          cp deploy/pilot/.env.example deploy/pilot/.env
          docker compose -f deploy/pilot/docker-compose.yml config -q
          echo "compose config OK"

  consensus-evidence:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Reproducible consensus 3-of-3 evidence (regeneration + scenarios)
        run: |
          # Regenerates the evidence in a temp dir and fails on any drift from
          # the committed artifacts (proves reproducibility) and on any scenario
          # that does not fail closed.
          python evidence/consensus_3of3/harness.py --check

      - name: Independent re-verification (no mcc_core)
        run: |
          # Re-checks MANIFEST integrity and independently re-derives every
          # consensus verdict using only cryptography + stdlib.
          python evidence/consensus_3of3/verify_independent.py

  consensus-enforcement:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Mandatory consensus 3-of-3 enforced in the execution path
        run: |
          # The coordinator runs with require_consensus=True: no governed action
          # actuates without a valid 3-of-3 bound to the token's
          # action/actor/payload/resource/policy_hash/nonce. Proves a valid 3-of-3
          # reaches the downstream once and every invalid or incomplete case
          # (missing, <3, veto, duplicate, untrusted, bad-sig, expired,
          # action/actor/resource/payload/nonce/policy mismatch, replay) is denied
          # and never forwarded — at the coordinator, over HTTP, and in the builder.
          pytest tests/test_consensus_enforcement.py \
                 tests/test_consensus_enforcement_http.py \
                 tests/test_consensus_builder.py -v

  consensus-challenge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Consensus challenge / nonce handshake (gateway issues the nonce)
        run: |
          # The gateway mints the one-time nonce inside a single-use, TTL-bounded
          # challenge bound to the exact operation; clients never generate it.
          # Proves: a valid challenge-backed 3-of-3 reaches the downstream once;
          # reused/expired/unknown challenges and every binding mismatch (nonce,
          # action, actor, resource, payload, policy) are denied and never
          # forwarded; a client-supplied nonce without a challenge cannot
          # actuate. Covers the service, coordinator, and multi-instance store.
          pytest tests/test_challenge.py \
                 tests/test_challenge_coordinator.py \
                 tests/test_challenge_http.py \
                 tests/test_challenge_redis.py -v

  nonce-redis-smoke:
    runs-on: ubuntu-latest
    services:
      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 5
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Cross-instance replay protection against a real Redis
        env:
          MCC_REDIS_URL: redis://127.0.0.1:6379/0
        run: |
          # Two independent gate instances share one Redis; a token verified on
          # one must be rejected when replayed on the other. Exits non-zero on
          # any miss.
          python scripts/redis_nonce_smoke.py

      - name: Cross-instance idempotency + aggregate ceiling against a real Redis
        env:
          MCC_REDIS_URL: redis://127.0.0.1:6379/0
        run: |
          # An operation executed on one instance cannot execute again on
          # another, and a cumulative velocity ceiling holds across separately-
          # signed transactions and instances. Exits non-zero on any miss.
          python scripts/redis_governance_smoke.py

      - name: Cross-instance mandate revocation against a real Redis
        env:
          MCC_REDIS_URL: redis://127.0.0.1:6379/0
        run: |
          # A mandate revoked through a shared Redis revocation list on one
          # instance must verify as REVOKED on another. Exits non-zero on miss.
          python scripts/redis_mandate_smoke.py

      - name: Cross-instance single-use approval consume against a real Redis
        env:
          MCC_REDIS_URL: redis://127.0.0.1:6379/0
        run: |
          # An approved request can be consumed exactly once even when two
          # instances race to consume it. Exits non-zero on any miss.
          python scripts/redis_approval_smoke.py

      - name: Cross-instance single-use consensus challenge against a real Redis
        env:
          MCC_REDIS_URL: redis://127.0.0.1:6379/0
        run: |
          # A challenge issued on one instance is visible on another and can be
          # consumed exactly once even when two instances race. Exits non-zero
          # on any miss.
          python scripts/redis_challenge_smoke.py

      - name: Governance HTTP layer cross-instance against a real Redis
        env:
          MCC_REDIS_URL: redis://127.0.0.1:6379/0
        run: |
          # Two GovernanceService instances share one Redis; a mandate revoked on
          # one is blocked on the other, and an approval consumed on one cannot be
          # replayed on the other — through the full coordinator+gate path.
          python scripts/redis_governance_http_smoke.py

      - name: Hardened shared Redis governance state against a real Redis
        env:
          MCC_REDIS_URL: redis://127.0.0.1:6379/0
        run: |
          # Canonical namespaced keys; nonce single-use cross-instance; idempotency
          # conflict + single concurrent winner; velocity aggregate enforced across
          # instances with the ATOMIC LUA reserve under real concurrency; negative
          # amount rejected. Proves the Lua actually runs in Redis. Exits non-zero
          # on any miss.
          python scripts/redis_runtime_smoke.py

      - name: Governed-agent example multi-instance against a real Redis
        env:
          MCC_REDIS_URL: redis://127.0.0.1:6379/0
        run: |
          # The end-to-end governed-agent demo against a real Redis: a nonce
          # consumed by one MCC instance is rejected by a second instance sharing
          # the same Redis, and a gateway-issued consensus challenge is single-use
          # across instances (the @skipif real-Redis tests run here). pytest is a
          # dev dependency.
          pip install -r requirements-dev.txt
          pytest tests/examples/ -q

  invariants:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - name: No HMAC in authority-bearing runtime path
        run: |
          if grep -rin "hmac" main.py src/; then
            echo "STOP: HMAC found in authority-bearing runtime path"
            exit 1
          fi
          echo "OK: no HMAC in main.py or src/"

      - name: No legacy mcc-prior-auth string in runtime path
        run: |
          if grep -rn "mcc-prior-auth" main.py src/ tests/ scripts/; then
            echo "STOP: legacy repo-name typo found"
            exit 1
          fi
          echo "OK: no mcc-prior-auth string"

      - name: Fail-closed references present
        run: |
          grep -qi "fail-closed" main.py
          grep -qi "fail-closed" src/mcc_core/gate.py
          grep -qi "fail" src/mcc_core/nonce.py
          echo "OK: fail-closed posture referenced in runtime and gate"

      - name: Audit fsync path present
        run: |
          grep -q "fsync" src/mcc_core/audit.py
          echo "OK: fsync present in audit log"

      - name: Runtime version drift-guard (canonical VERSION = 1.11.0)
        run: |
          # Every product/runtime entrypoint must derive its version from the
          # single canonical VERSION file (no hardcoded literal). Fails on drift.
          # Frozen historical (v1.10.1), schema, doctrine, and README strings are
          # intentionally NOT inspected.
          python scripts/check_version_drift.py
          # Negative control: a deliberate hardcoded runtime literal must be caught.
          cp gateway/app.py /tmp/app.bak
          sed -i 's/version=RUNTIME_VERSION,/version="9.9.9-drift",/' gateway/app.py
          if python scripts/check_version_drift.py; then
            echo "STOP: drift-guard failed to catch a hardcoded runtime version"; cp /tmp/app.bak gateway/app.py; exit 1
          fi
          cp /tmp/app.bak gateway/app.py
          python scripts/check_version_drift.py
          echo "OK: drift-guard catches runtime drift and passes when canonical"

      - name: Frozen historical version untouched (v1.10.1)
        run: |
          # v1.10.1 is frozen historical/exhibit evidence. The runtime
          # reconciliation must never rewrite it. Confirm it is still present.
          grep -q "v1.10.1" CLAUDE.md && echo "OK: v1.10.1 frozen reference intact" \
            || { echo "STOP: frozen v1.10.1 reference was modified"; exit 1; }

      - name: No fail-open exception handlers in gate
        run: |
          if grep -rn "except.*ALLOW\|except.*allow" src/mcc_core/gate.py main.py; then
            echo "STOP: fail-open pattern detected"
            exit 1
          fi
          echo "OK: no fail-open handlers"

      - name: YAML policy thresholds match Rego canon
        run: |
          rego=$(grep -oE 'amount (<=|>) [0-9]+' policies/mcc.rego | grep -oE '[0-9]+' | sort -un)
          if [ -z "$rego" ]; then
            echo "STOP: no thresholds found in policies/mcc.rego"
            exit 1
          fi
          for f in mcc.yaml policies.yaml; do
            yaml=$(grep -oE 'amount (<=|>) [0-9]+' "$f" | grep -oE '[0-9]+' | sort -un)
            if [ "$rego" != "$yaml" ]; then
              echo "STOP: $f thresholds do not match rego canon"
              echo "rego: $rego"
              echo "$f: $yaml"
              exit 1
            fi
          done
          echo "OK: mcc.yaml and policies.yaml match rego canon thresholds:"
          echo "$rego"
